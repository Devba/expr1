<!DOCTYPE html>
<html lang="es">
    <%- include('layout2', {user: "lll"}); %>

    <body style="visibility: hidden;">
    <%- include('inicio/swtests', {user: "lll"}); %>

<link rel='stylesheet' href='stylesheets/style.css' />

<script>

   //This is the landing web page
       function loadDoc(req) {
          Swal.showLoading();
            var xhttp = new XMLHttpRequest();
            xhttp.onreadystatechange = function() {
              
                if (this.readyState == 4 && this.status == 200) {
                    console.log(this.response);
                    if (this.response=="Not a valid License"){                      //Swal.hideLoading();
                        miform.icon="error"
                        Swal.fire(miform).then(function(result) {
                            if (result.isConfirmed) {
                              
                              var res=loadDoc(result.value);                      //  Swal.showValidationMessage("algo ha pasado");
                              console.log(res.value)                              //location.assign("https://stackoverflow.com/")
                            }
                        })
                        Swal.showValidationMessage(this.response + ",Please Log in again ")
                      }else{
                    window.location.replace("/start?lic=1")
                    }
      
                }
            };
             xhttp.open("POST", "/checkLic", true); //xhttp.setRequestHeader('Content-type', 'application/x-www-form-urlencoded');
            //const formData = new FormData("f1");
            xhttp.setRequestHeader("Content-Type", "application/json;charset=UTF-8");
            xhttp.send(JSON.stringify({ "license": req, "response": { "name": "Tester" } })); //var params = req;            //xhttp.send(req);

        }
   
    

$( document ).ready(function() {

    console.log( "inicio ready!" );

    var tempDivElement = document.createElement("div");
    tempDivElement.innerHTML = miform;    //var mf=miform.replace(/(\r\n|\n|\r)/gm, "");


    Swal.fire(miform).then(function(result) {
        if (result.isConfirmed) {
          var res=loadDoc(result.value);       //  Swal.showValidationMessage("algo ha pasado");
          console.log("swal back log ")//location.assign("https://stackoverflow.com/")
        }
      })
      })

   let domReady = (cb) => { // para visibilizar el body
       document.readyState === 'interactive' || document.readyState === 'complete'
           ? cb()
           : document.addEventListener('DOMContentLoaded', cb);
   };

   domReady(() => {
       // Display body when DOM is loaded
       document.body.style.visibility = 'visible';
   });


</script>

    <div class="container-fluid no-padding">



    <div class="row">
        <div class="col-md-8 col-mod-offset-3">
            fdkjkdjck
            <div class="ajax-loader "><img src="giphy.gif" class="img-responsive" /></div>
        </div>
    </div>
    </div>
    </body>
</html>
